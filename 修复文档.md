# MijiaPowerPlugin 修复文档(v1.1.1 → v1.1.3)

> 审查日期:2026-09-13
> 范围:代码审查发现的全部 P1/P2 问题及可安全修复的 P3 问题。
> 原则:小步修改,不引入第三方依赖,保持与 TrafficMonitor 插件机制、旧配置文件的兼容。

## 一、问题总览

| 编号 | 严重度 | 问题 | 处理 |
|------|--------|------|------|
| P1-1 | 高 | 功率历史在 TrafficMonitor 正常退出时不落盘,只有改配置时才保存 | 修复 |
| P1-2 | 高 | "清除历史"只删文件不清内存,旧数据稍后被写回(复活) | 修复 |
| P1-3 | 高 | 历史文件按"位置索引"命名,删除/重排设备后历史错位 | 修复 |
| P1-4 | 高 | 设备离线时点"确定"冻结界面,最长约 10 秒/设备 | 修复 |
| P2-5 | 中 | UDP 响应不校验来源与完整性,同网段可伪造功率值 | 修复 |
| P2-6 | 中 | 配置对象被 UI 线程写、采集线程无锁读,构成数据竞争(UB) | 修复 |
| P2-7 | 中 | `DataRequired` 兼容回退用进程当前目录作配置目录 | 修复 |
| P2-8 | 中 | 功率值为字符串型(`"value":"23.4"`)时解析失败,无限重连 | 修复 |
| P2-9 | 中 | 非法 Token 被静默接受(全 0 密钥),只能看到笼统的"连接失败" | 修复 |
| P2-10 | 中 | README 称历史记录"默认关闭",代码默认开启 | 修复(文档) |
| P2-11 | 中 | 实时缓冲 300 条,1 秒间隔下"最近10分钟"统计不足 | 修复 |
| P3-12 | 低 | 死代码:`MiioAES::xtime`、`AddCtrl`、`GetTotalWatts`、`SaveStateHistory`、`ConnectOne`、`IDC_SCROLL_CONTAINER` | 清理 |
| P3-13 | 低 | `WSAStartup` 每次收发调用且从不 `WSACleanup` | 修复 |
| P3-14 | 低 | 设置对话框每次打开泄漏一个 GDI 字体对象 | 修复 |
| P3-15 | 低 | `test_dialog.cpp` 不检查 argc,无参运行崩溃 | 修复 |
| P3-16 | 低 | `build_gcc.sh` 硬编码用户目录,换机器只能靠 find 兜底 | 修复 |
| P3-17 | 低 | IDC 宏在 OptionsDlg.cpp 与 resource.h 重复定义;`TRAFFICE_MONITOR_PLUGIN` 拼写;`MAX_DEVICES` 建议 `inline constexpr` | 修复 |

审查中记录、**有意不修**的项(说明理由):

- "测试连接"在 UI 线程同步等待最长 5 秒:设置对话框内可接受的交互,已先绘制"正在连接..."提示;
- 设置窗口 `WS_EX_TOPMOST`:v1.1.1 有意为之;
- Token 明文存 INI:miIO 局域网方案的固有形态,README 已提示;
- 本机时钟大幅校准导致时间戳偏移:触发失败后自动重连自愈;
- 响应 msgId 匹配:每轮请求都新建 socket(新临时端口),不存在收到陈旧响应的路径,匹配反而有兼容风险(个别设备 JSON 带空格),故不加。

## 二、修复方案与涉及文件

### P1-1 历史数据正常退出不落盘

- **根因**:历史保存有三条路径,正常退出时都走不到——
  1. `SampleLoop` 退出前保存:需要 `m_stopFlag` 置位,而 `Shutdown()` 只在 `DLL_PROCESS_DETACH`(loader lock,此时采样线程已被系统终止)时被调用;
  2. 析构函数保存:`g_pluginInstance` 从不 `delete`,死代码。
- **方案**:利用主程序周期调用(约 1 秒一次)、运行于主线程的 `DataRequired()`,做 60 秒节流的周期落盘(`m_lastHistorySave` 节流,`StartSampling` 时初始化)。采样线程退出保存与析构保存保留作兜底。
- **效果**:最坏丢失 1 分钟的分钟级采样;崩溃/断电同样只丢 1 分钟。
- **文件**:`MijiaPowerPlugin.h/.cpp`

### P1-2 清除历史后数据复活

- **根因**:`OptionsDlg.cpp` 只删除磁盘 JSON,内存中各 `DeviceState::history` 的 `m_realtime/m_longterm` 未清空,之后的落盘把旧数据写回。
- **方案**:`PowerHistory` 新增 `Clear()`;插件新增 `ClearAllHistory()`(先清内存、再删除磁盘文件,包括旧命名文件),对话框通过 `COptionsDlg::Show(hParent, plugin)` 拿到插件指针调用。先清内存后删文件,保证与周期落盘并发时最终一致(最多留下一个空 `[]` 文件)。
- **文件**:`PowerHistory.h/.cpp`、`MijiaPowerPlugin.h/.cpp`、`OptionsDlg.h/.cpp`

### P1-3 历史文件按位置索引命名导致错位

- **根因**:`GetHistoryFilePath(index)` 用序号命名,`InitDevices` 按当前索引加载。配置 `[A,B]` 产生 `history_1/2`;删除 A 或调换顺序后重启,B 加载到 A 的数据。
- **方案**:历史文件改为按设备身份(IP)命名:`MijiaPower_history_<IP>.json`(同一 IP 配多个设备时追加序号防覆盖;IP 为空退回按索引命名)。保留旧命名兼容:
  - v1.0 `MijiaPower_history.json` → 第 1 个设备(原有迁移保留);
  - v1.1.0/1.1.1 `MijiaPower_history_N.json` → 对应设备的按 IP 文件;
  - 迁移均为"目标不存在才迁移",可重复执行。`ApplyNewConfig` 中被移除设备的保存改为按其旧身份 IP 取路径。
- **文件**:`PluginConfig.h/.cpp`、`MijiaPowerPlugin.cpp`(LazyInit/InitDevices/ApplyNewConfig/各保存点)

### P1-4 点"确定"冻结界面

- **根因**:`ApplyNewConfig`(主线程)等待 `st->mtx`,而采样线程在 `PollOne/TryConnectLocked` 中持同一把锁做网络 I/O(握手 5s + 收包 5s 超时)。
- **方案**:`st.mtx` 职责收窄为仅保护 `cfg` 的读写;网络 I/O 全部移到锁外(`st.device` 只由采样线程访问,文档化该约定);`ConnectOne` 并入 `PollOne`。`ApplyNewConfig` 对锁的持有时间降到微秒级。
- **文件**:`MijiaPowerPlugin.h/.cpp`

### P2-5 UDP 来源与完整性校验

- **方案**:
  1. socket 对设备地址 `connect()`:内核只投递来自该设备 IP:54321 的包,天然过滤同网段伪造源;
  2. `inet_pton`/`send` 返回值检查;
  3. `Handshake` 校验应答长度(=32)与魔数(`0x2131`);
  4. `ParsePacket` 校验魔数、device_id、以及 `checksum == MD5(header + token + payload)`,任一不符按失败处理(触发重连)。
- **文件**:`MiioDevice.cpp`

### P2-6 配置对象数据竞争

- **根因**:`SaveFromDialog`(UI 线程)直接改写 `ConfigManager::Get()` 返回的引用;`SampleLoop`(采样线程)每秒无锁读 `updateIntervalSec/enableRecording`。
- **方案**:`ConfigManager` 增加 `mutable std::mutex`:`Get()` 返回**副本**、新增 `Set(const PluginConfig&)` 加锁写回;`Load/Save/GetHistoryFilePath` 内部加锁。所有调用点从 `auto& cfg = ...Get()` 改为 `auto cfg = ...Get()`;`SaveFromDialog` 改为"副本上修改 → Set → Save"。锁序固定为 devices → config → history,无反向嵌套。
- **文件**:`PluginConfig.h/.cpp`、`MijiaPowerPlugin.cpp`、`OptionsDlg.cpp`

### P2-7 配置目录回退用 cwd

- **方案**:`DataRequired` 兜底时优先用 `m_pTM->GetPluginConfigDir()`——`m_pTM` 非空即主程序支持 API v7(虚表完整),调用安全;仅当不可用时才退回 `GetCurrentDirectoryW`。
- **文件**:`MijiaPowerPlugin.cpp`

### P2-8 字符串型功率值解析失败

- **方案**:`GetPower` 解析 `"value":` 后,若首字符为引号则按字符串提取再 `stod`;数字路径不变。
- **文件**:`MiioDevice.cpp`

### P2-9 非法 Token 静默接受

- **方案**:`MiioDevice` 构造时校验 token 为 32 位十六进制(`HexToBytes` 返回值记入 `m_tokenValid`),失败时 `Send` 直接返回 false(不产生网络流量);设置对话框"测试连接"先校验格式,给出"应为 32 位十六进制字符串"的明确提示。
- **文件**:`MiioDevice.h/.cpp`、`OptionsDlg.cpp`

### P2-10 README 默认值不一致

- **方案**:以代码为准(历史记录默认开启),修正 README 表格;同时更新 README 中的历史文件命名说明与更新日志。
- **文件**:`README.md`

### P2-11 实时缓冲不足

- **方案**:`MAX_REALTIME` 300 → 600(1 秒间隔下覆盖 10 分钟;8 设备下内存增量可忽略)。
- **文件**:`PowerHistory.h`

### P3 各项

- 删除死代码:`MiioAES::xtime`、`AddCtrl`、`GetTotalWatts`、`SaveStateHistory`、`ConnectOne`(并入 PollOne)、`IDC_SCROLL_CONTAINER`;
- `WSAStartup` 移到 `MiioDevice` 构造函数、`WSACleanup` 移到析构函数,配对引用计数;
- 对话框字体句柄存入 `DlgState`,`Show` 结束销毁窗口后 `DeleteObject`(兜底字体为 stock 对象,不删);
- `test_dialog.cpp` 增加 argc 检查;
- `build_gcc.sh` 优先从 `PATH` 找 g++,找不到再在 WinGet 目录($USERNAME 展开)查找;
- IDC 宏统一收敛到 `resource.h`(补 1016–1019),`OptionsDlg.cpp` 不再重复定义;`TRAFFICE_MONITOR_PLUGIN` 改 `TRAFFIC_MONITOR_PLUGIN`;`MAX_DEVICES` 改 `inline constexpr`;
- 版本号 `TMI_VERSION` 1.1.1 → **1.1.2**,README 增加对应更新日志。

## 三、锁与线程模型(修复后)

| 数据 | 保护方式 | 访问者 |
|------|----------|--------|
| `DeviceState::cfg` | `st.mtx`(仅短暂快照/更新) | 采样线程(读)、主线程 ApplyNewConfig(写) |
| `DeviceState::device` | 仅采样线程访问,无锁 | 采样线程 |
| `DeviceState::connected/watts` | `std::atomic` | 任意线程 |
| `PowerHistory` 内部数据 | 自带 `m_mutex` | 采样线程(写)、主线程(保存/清空) |
| `ConfigManager::m_cfg` | 自带 `m_mutex`,`Get()` 返回副本 | UI 线程(读写)、采样线程(读) |
| `m_devices` 列表 | `m_devicesMutex`,快照共享指针 | UI 线程(改)、采样线程与显示项(读) |

锁序:`m_devicesMutex` → `ConfigManager::m_mutex` → `PowerHistory::m_mutex`,不存在反向嵌套。

## 四、验证记录(2026-09-13 实测)

1. **构建**:MinGW g++ 16.1.0,`build_gcc.sh` 构建通过,产出 `bin/Release/x64/MijiaPower.dll`(静态链接,1.18 MB);另以 `-Wall -Wextra` 严格编译,项目自身代码 0 警告(仅上游 `PluginInterface.h` 的 unused-parameter 警告,属正常)。
2. **冒烟测试**(`test_harness`,配置 2 台不可达测试设备 192.0.2.x):插件加载正常,API v7、版本号 1.1.2;显示项枚举正确(`MijiaPwr1`/`MijiaPwr2`/`MijiaPwrTotal`,2 设备时总功率项出现);标签、数值(`连接中...`)、tooltip(未连接状态 + IP)输出正确;10 秒采样期间无崩溃。`test_dialog` 编译通过(交互界面未自动化验证)。
3. **P1-3 迁移验证**:预置 v1.1.1 按索引命名的 `MijiaPower_history_1.json`、`MijiaPower_history_2.json`,首次加载后自动重命名为 `MijiaPower_history_192.0.2.10.json`、`MijiaPower_history_192.0.2.11.json`,文件内容逐字节保留。
4. **P1-1 周期落盘验证**:编写测试宿主模拟主程序每秒调用 `DataRequired` 共 130 秒,历史文件 mtime 在 60 秒节流到期后被更新(共触发两次),证明周期保存按预期工作。
   > 排查记录:最初用"文件内容是否变化"判断,误判为未触发。原因有二——测试无真实设备、无新增采样,保存内容与启动时加载的迁移内容完全相同;调试日志的注入锚点意外命中了析构函数中几乎相同的保存语句。改用文件 mtime 判定后确认功能正常。此过程顺带单独验证了 `PowerHistory::SaveToFile` 在 MinGW 下宽字符路径写入正常。
5. **遗留说明**:`bin/Release/x64/MijiaPower.dll` 是 git 已跟踪文件(`.gitignore` 对已跟踪文件不生效),本次构建已更新该文件;提交时可用 `git rm --cached bin/ -r` 取消跟踪,使忽略规则真正生效。

## 五、修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `MiioDevice.h/.cpp` | UDP connect 过滤来源、包校验(魔数/设备ID/checksum)、Token 校验、WSAStartup 配对、字符串型 value 解析、Encrypt 入参上限、删除 xtime |
| `PluginConfig.h/.cpp` | 配置读写加锁(Get 副本/Set)、历史文件按 IP 命名 + 旧命名查询接口、`inline constexpr MAX_DEVICES` |
| `PowerHistory.h/.cpp` | 新增 `Clear()`、`MAX_REALTIME` 300→600 |
| `MijiaPowerPlugin.h/.cpp` | `DataRequired` 周期落盘 + 配置目录回退改进、网络 I/O 移出 `st.mtx`、`ClearAllHistory`、历史迁移扩展到按索引命名文件、删除 `GetTotalWatts`/`SaveStateHistory`/`ConnectOne`、版本号 1.1.2 |
| `OptionsDlg.h/.cpp` | `Show` 接收插件指针、"清除历史"联动清内存、Token 格式提示、GDI 字体释放、删除重复 IDC 宏与 `AddCtrl`、`SaveFromDialog` 经 Set 写回 |
| `resource.h` | 收敛全部控件 ID 定义(补 1016–1019) |
| `pch.h` | 修正 `TRAFFIC_MONITOR_PLUGIN` 拼写 |
| `test_dialog.cpp` | argc 检查、main 签名 |
| `build_gcc.sh` | g++ 查找路径可移植化 |
| `README.md` | 历史文件命名说明、默认值修正、v1.1.2 更新日志 |
| `修复文档.md` | 本文档(新增) |

## 六、第二轮审查与修复(v1.1.2 → v1.1.3,2026-09-14)

### 审查结论

- v1.1.2 全部修复逐条核对代码,均已落实;锁序 devices → config → history 推演无死锁路径,`MiioDevice` 仅采样线程访问的约定、`shared_ptr` 快照生命周期处理正确;
- 未发现新的 P1/P2 问题;遗留项均为 P3 级打磨,合并为一个 v1.1.3 小版本处理。

### 问题清单与处理

| 编号 | 问题 | 处理 |
|------|------|------|
| R2-1 | 设置界面历史文件说明仍写旧索引命名 `MijiaPower_history_N.json`(v1.1.2 已改按 IP 命名),用户按文案找不到文件 | 修复 |
| R2-2 | "清除历史"只删当前配置设备与旧命名文件,已移除设备遗留的 IP 命名历史文件永久残留 | 修复 |
| R2-3 | `Save()` 不清理 v1.0 遗留的 `[Device]` 段,旧 Token 明文多留一份 | 修复 |
| R2-4 | 对话框控件无 `WS_TABSTOP`(CreateWindow 不像对话框模板自动携带),Tab 导航失效 | 修复 |
| R2-5 | `InitDevices/ApplyNewConfig` 持 `m_devicesMutex` 期间做历史文件磁盘 I/O,阻塞采样线程的 SnapshotDevices | 修复 |
| R2-6 | Token 格式非法时任务栏永远显示"连接中...",区分不出配置错误与正在连接 | 修复 |
| R2-7 | `MiioAES::Decrypt` 去 PKCS7 padding 只按末字节裁剪,不校验填充字节一致性 | 修复 |
| R2-8 | 死代码:`MiioDeviceConfig/MiioProperty` 结构体无引用;`OptionsDlg.h` 残留 DialogBoxParam 旧方案的 4 个成员声明 | 清理 |
| R2-9 | `test_dialog.cpp` 未检查 `GetProcAddress` 返回值,无导出 DLL 时空指针崩溃 | 修复 |
| R2-10 | `RegisterDlgClass` 注释称"线程安全"但静态 ATOM 无锁(实际仅主线程调用,无碍) | 修复(注释) |
| R2-11 | `Send()` 的 result 提取约定(错误响应返回整个 JSON 且返回 true、由上层兜底)无注释说明 | 修复(注释) |
| R2-12 | README 未说明"最近10分钟"统计基于内存实时缓冲,TrafficMonitor 重启后需重新积累 | 修复(文档) |

### 修复要点

- **R2-2**:`ConfigManager` 新增 `GetAllHistoryFilePaths()`(按 `MijiaPower_history*.json` 模式枚举配置目录),`ClearAllHistory` 改为按模式全删,覆盖当前设备、同 IP 多设备的序号文件、已移除设备遗留文件与全部旧命名文件;
- **R2-5**:两个函数改为"锁外完成历史加载/保存 → 最后仅短暂持锁换血";读取被移除设备的旧 IP 时按 `cfg` 保护约定短暂持 `st.mtx`;
- **R2-6**:`PluginConfig.h` 新增共用 `IsValidToken()`(设置对话框的校验改为复用,删除重复实现);`GetItemValueText` 对非法 Token 显示"未配置"(宽度小于样例文本,无裁切风险);
- **R2-7**:全部填充字节一致才裁剪;末字节不在 1~16(如 0 填充固件)则不裁剪,交由去尾部 `\0` 逻辑;填充不一致按数据损坏返回失败。

### 有意不修(与第一轮衔接)

- `WSAStartup/WSACleanup` 随每个 `MiioDevice` 构造/析构:引用计数保证正确性,仅重连频繁时略低效,进程级单次初始化的收益不值得引入新的全局状态;
- "测试连接"同步等待 5 秒、设置窗口 `WS_EX_TOPMOST`、Token 明文存 INI、时钟校准导致时间戳偏移、不匹配响应 msgId:同第一轮结论。

### 验证记录(2026-09-14 实测)

1. **构建**:`build_gcc.sh` 构建通过(1.22 MB);`-Wall -Wextra` 严格编译,项目自身代码 0 警告。
2. **冒烟测试**(test_harness,2 台不可达测试设备 192.0.2.x + 预置旧索引命名历史文件):插件加载正常,API v7、版本号 1.1.3;显示项枚举正确;`MijiaPower_history_1.json` 自动迁移为 `MijiaPower_history_192.0.2.10.json`;10 秒采样期间无崩溃。
3. **部署**:`plugins/MijiaPower.dll` 已同步更新;覆盖成功说明运行中的 TrafficMonitor 未映射该 DLL,重启后加载 v1.1.3。

### 修改文件清单(第二轮)

| 文件 | 修改内容 |
|------|----------|
| `PluginConfig.h/.cpp` | 新增 `IsValidToken()`、`GetAllHistoryFilePaths()`;`Save()` 清理 v1.0 `[Device]` 段 |
| `MijiaPowerPlugin.cpp` | `ClearAllHistory` 按模式删除全部历史文件、`InitDevices/ApplyNewConfig` 磁盘 I/O 移出锁、非法 Token 显示"未配置"、版本号 1.1.3 |
| `MiioDevice.h/.cpp` | 删除无引用结构体、PKCS7 填充一致性校验、`Send()` 约定注释 |
| `OptionsDlg.h/.cpp` | 历史文件说明文案更正、控件补 `WS_TABSTOP`、改用共用 `IsValidToken`、"清除历史"兜底分支改按模式删除、删除残留声明与重复 Token 校验、注释更正 |
| `test_dialog.cpp` | `GetProcAddress` 空指针检查 |
| `README.md` | v1.1.3 更新日志、"最近10分钟"内存缓冲说明 |
| `修复文档.md` | 追加本轮记录 |

## 七、第三轮查缺补漏(v1.1.3 → v1.1.4,2026-09-14)

| 编号 | 问题 | 处理 |
|------|------|------|
| R3-1 | **高**:README 示例 ini 中 `[Device1]` 的 Token 与部署配置中真实设备(总控,192.168.1.100)的 Token 一致,且已随仓库推送至公开 GitHub;示例 IP 亦为真实局域网网段 | 修复(示例改虚构值) |
| R3-2 | `插件截图.png` 含真实设备名(电脑/总控/空调)与真实 IP,且画面中的对话框说明文字仍是 v1.1.2 之前的旧文案 | 修复(用 test_dialog + 虚构配置重拍) |
| R3-3 | `PowerHistory::LoadFromFile` 无读取上限,异常膨胀的历史文件会全量载入内存 | 修复(4M 字符上限,约为正常数据 13 倍余量) |
| R3-4 | v1.1.3 更正后的历史说明文字在 300px 宽控件内仍被省略号截断,截断点恰在文件名模式处 | 修复(精简为"历史按设备 IP 命名,保存在插件配置目录中",单行完整显示) |

**关于 R3-1 的风险说明**:泄露的 miIO Token 仅在与设备同一局域网内可用,公网无法利用,实际风险低;但该 Token 已存在于公开的 git 历史(替换 HEAD 不清除历史),彻底处理需在米家 App 中重新配网轮换 Token,或重写仓库历史(需强推)。本文档按"替换现值 + 说明风险"处理。

### 验证记录(2026-09-14)

1. `build_gcc.sh` 构建通过;`-Wall -Wextra` 严格编译项目自身代码 0 警告;
2. test_harness 冒烟测试正常(版本号 1.1.4,枚举与迁移行为不变);
3. 新截图由 test_dialog.exe 加载虚构配置(192.0.2.x / 假 Token)实拍,PowerShell 截取窗口区域;test_dialog 增加 `SetProcessDPIAware()`,高 DPI 下按原生像素渲染,避免截图模糊与坐标虚拟化错位。
